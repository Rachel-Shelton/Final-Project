import React from "react";
import styled from "styled-components";

const Navigation = () => {
  return <NavBar>Navigation</NavBar>;
};

const NavBar = styled.div`
  margin-top: 5vh;
`;
export default Navigation;
